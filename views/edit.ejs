<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            background: #000;
            color: white
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 10px;
            width: 400px;
            padding: 30px;
            border: 1px solid white;
            margin: 20px auto;

        }
        form input {
            height: 30px;
            border-radius: 1rem;
            border: none;
            padding-left: 10px;

        }
        form button {
            height: 30px;
            border-radius: 1rem;
            border: none;
            background: orangered;
            color: white;
            cursor: pointer;
        }
        .password {
            max-width: 100px;
            overflow-x: scroll;
        }
        table {
            width: 100%;
            
        }
        td,th{
            border: 1px solid gray;
            border-radius: 1rem;
            padding-left: 10px;
            height: 40px;
        }
        h3 {
            text-align: center;
        }
    </style>
</head>
<body>
    <form action="/edit/<%= filtered[0].id %>" method="post">
        <h3>Edit User <%= filtered[0].id %></h3>     
        <input type="text" name="name" value="<%= filtered[0].name %>">
        <input type="number" name="age" min="14" value="<%= filtered[0].age %>">
        <input type="email" name="email" value="<%= filtered[0].email %>">
        <input type="text" name="password" value="<%= filtered[0].password %>">
        <button>
            Update
        </button>
    </form>
</body>
</html>